<Window x:Class="QuizHelper.Windows.WindowSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="창 선택"
        Width="400"
        Height="450"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#444" BorderThickness="1">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="🪟 캡처할 창 선택"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <Button x:Name="CloseButton"
                        Content="✕"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        Foreground="#888"
                        BorderThickness="0"
                        FontSize="14"
                        Cursor="Hand"
                        Width="24" Height="24"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Refresh Button -->
            <Button x:Name="RefreshButton"
                    Grid.Row="1"
                    Content="🔄 창 목록 새로고침"
                    Background="#333"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="10,8"
                    Margin="0,0,0,10"
                    Cursor="Hand"
                    Click="RefreshButton_Click"/>

            <!-- Window List -->
            <Border Grid.Row="2" Background="#2A2A2A" CornerRadius="6" Padding="5">
                <ListBox x:Name="WindowListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="White"
                         SelectionMode="Single"
                         MouseDoubleClick="WindowListBox_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📺" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" 
                                               Foreground="White" 
                                               FontSize="13"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Foreground="#888" FontSize="11">
                                        <Run Text="{Binding Bounds.Width, Mode=OneWay}"/>
                                        <Run Text="x"/>
                                        <Run Text="{Binding Bounds.Height, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="4" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#0066CC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <!-- Buttons -->
            <Grid Grid.Row="3" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton"
                        Grid.Column="0"
                        Content="취소"
                        Background="#444"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="0,12"
                        Cursor="Hand"
                        Click="CancelButton_Click"/>

                <Button x:Name="SelectButton"
                        Grid.Column="2"
                        Content="선택"
                        Background="#0066CC"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="0,12"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="SelectButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
