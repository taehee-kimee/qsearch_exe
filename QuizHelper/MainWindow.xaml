<Window x:Class="QuizHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz Helper"
        Width="350"
        Height="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#CC000000" CornerRadius="12" Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Title and Close Button -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Quiz Helper"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <Button x:Name="CloseButton"
                        Content="✕"
                        Style="{StaticResource CloseButton}"
                        HorizontalAlignment="Right"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Control Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="SelectAreaButton"
                        Content="📍 Select Area"
                        Style="{StaticResource TransparentButton}"
                        Click="SelectAreaButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="StartStopButton"
                        Content="▶ Start"
                        Style="{StaticResource TransparentButton}"
                        Click="StartStopButton_Click"
                        IsEnabled="False"/>
            </StackPanel>

            <!-- Result Display Area -->
            <Border Grid.Row="2"
                    Background="#22FFFFFF"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ResultText"
                               Text="Select an area and press Start to begin scanning..."
                               Foreground="#AAFFFFFF"
                               FontSize="14"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               VerticalAlignment="Center"/>
                </ScrollViewer>
            </Border>

            <!-- Status -->
            <TextBlock x:Name="StatusText"
                       Grid.Row="3"
                       Text="Status: Ready"
                       Foreground="#88FFFFFF"
                       FontSize="11"
                       Margin="0,0,0,8"/>

            <!-- Opacity Slider -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Opacity:"
                           Foreground="#88FFFFFF"
                           FontSize="11"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>

                <Slider x:Name="OpacitySlider"
                        Grid.Column="1"
                        Minimum="0.2"
                        Maximum="1.0"
                        Value="1.0"
                        VerticalAlignment="Center"
                        ValueChanged="OpacitySlider_ValueChanged"/>

                <TextBlock x:Name="OpacityValueText"
                           Grid.Column="2"
                           Text="100%"
                           Foreground="#88FFFFFF"
                           FontSize="11"
                           VerticalAlignment="Center"
                           Width="35"
                           TextAlignment="Right"/>
            </Grid>
        </Grid>
    </Border>
</Window>
